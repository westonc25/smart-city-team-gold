import { t } from 'elysia'
import { type UnwrapSchema } from 'elysia'

// Schemas

export const Session = {
    
} as const

export namespace Session {
    // export type _____ = UnwrapSchema<typeof Session._____>
    
}